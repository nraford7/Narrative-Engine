# Independent final review

16 September 2026. Scope: Narrative-Engine, `pre-spine-rebuild` through the final reviewed working tree. This reviewer made no runtime edits or commits; this review artifact is the only repository write.

**Verdict: READY for local integration. No unresolved material implementation or acceptance-report blocker remains.** This verdict does not authorize a remote push and does not establish performance under an unchanged historical Claude generator.

## Findings and closure

The independent first pass found four material contract gaps. All are closed in the actual current files:

1. Required prose craft reintroduced catalog loading and mandatory figured/developed-sentence production. Seven finite NE adaptations now remove those instructions, retain Floor/Filter, and keep Ceiling optional. The sync checker fails if a canonical adaptation anchor changes. Humanizing open-thread and ELI5 analogy quotas were also made conditional.
2. Escalation cleanup deleted reports needed for the user's decision. Current file hygiene and shared-loop cleanup preserve unresolved reports and sidecar; resolved reports are archived before trigger removal.
3. A user-authorized framework restart could encounter stale mismatch triggers and enter revision mode. The restart now archives and clears trigger/prior files, resets draft evidence state, and preserves the run-level focal-reset limit.
4. A first evidence audit dispatched by an earlier repair could receive only changed sections, and its run state depended on deletable reports. The first audit now explicitly covers the entire draft; later audits receive count and archived report independently of trigger presence, check changes/prior findings, and reuse CLEAN coverage when the draft is unchanged.

The initially incomplete protocol harness is now accurately labeled: `run.py` is a role runner, the executed transitions are retained, and `replay.py` assembles the full sequence. The README explicitly says the consolidated wrapper was syntax/guard checked rather than fully re-executed. This closes the misleading-replayability risk without claiming an unperformed run.

## Acceptance evidence checked

- Recomputed every boardroom arm/dimension mean and compared stored rows to raw judge JSON. Saved comparison rows are identical between the initial and final result records. Rebuilt outranks old A on 4/4 sources and B on 3/4; mean fidelity is 10.0 and naturalness 8.25.
- Initial prose naturalness 7.0 remains recorded as a failure. The sole repaired prose score matches the retained raw independent judge response: fidelity 9.5, naturalness 8.0. The final record retains the initial extra-format rows. Keynote clears both thresholds at 9.5/8.5.
- Inspected anonymous comparison prompts and the rerun input. External judges receive source, audience/ask and deliverable bodies; writer audits, arm names, mapping and prior scores are not included. The saved baselines remain reused rather than regenerated. Available generation/scoring events show no tool calls.
- Independently read command sequences in all four protocol judge transcripts: body read, cold-read write, source read, source-check append, then brief read. No sidecar read occurs in those sequences. The second focal leg remains NEEDS_REVISION plus advisory; it is not misreported as a delivery PASS. Evidence repair resolves the planted claim, receives CLEAN, then a fresh focal PASS; the evidence scenario records no remaining triggers and retains sidecar through delivery.
- The onboarding 96%/71% and preliminary 3–4x quotes satisfy the binding consequential-passage requirement. The narrower plan expectation of a 2.3x quote is explicitly identified as a deviation rather than concealed.
- Compared the ablation runtime manifest to current file hashes: no drift. Earlier protocol-role timing differences and the later explicit evidence-count dispatch are disclosed.
- Re-executed all 13 rebuild gates, all sync checks, and the three sync regression tests: PASS. These checks complement contract review; they do not independently prove generated-writing quality.

## Remaining limits and integration seams

The acceptance documents accurately bound the claims. Comparative generation used a different or unconfirmed historical generator and solo-simulated internal gates. Four sources, one sample per arm, and one external judge model support the measured outcomes, not causal attribution or future reliability. Separate isolated protocol roles establish the sampled file protocol. Escalation/cap breach and framework restart were reviewed mechanically and by contract rather than run as additional live model scenarios.

Hook means decline relative to both saved baselines, and the Astra boardroom output still ranks behind B. Both tradeoffs are retained in the report. Initial prose failure, one permitted repair, two excluded unjudged pre-fix generations, and no runtime change for the retry are disclosed. No further repair or rescore is warranted merely to improve the appearance of the results.

Runtime paths resolve to the reviewed Narrative-Engine repository. The continuation reports its separate 46-file keynote-create hash check; this reviewer made no changes there. Confidential Astra full text remains in private scratch and is not reproduced in this review. Final commit bookkeeping and the parent Agency evaluation remain the parent's integration tasks.

Recovery remains local and reversible: retain `pre-spine-rebuild` and the continuation base `9fa82c1`; revert the continuation commit to undo this completion work without resetting unrelated shared work. No publication is part of this verdict.
